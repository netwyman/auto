---
- hosts: "{{type}}"
  gather_facts: false
  connection: local
  #vars_files:
  #  - answerfile.yml
  roles:
     - vm-remove-name
  tasks: 
     - name: IP address info
       debug: 
         msg: "Server {{type}},is successfully removed!"
